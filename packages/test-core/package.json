{
  "name": "@hardware-test/test-core",
  "version": "0.1.0",
  "private": true,
  "description": "Core testing logic for hardware wallet testing",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts",
    "dev": "tsup src/index.ts --format cjs,esm --dts --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@onekeyfe/hd-core": "^1.0.0",
    "@onekeyfe/hd-transport": "^1.0.0",
    "@onekeyfe/hd-transport-http": "^1.0.0",
    "eventemitter3": "^5.0.1"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "vitest": "^1.2.0"
  }
}
